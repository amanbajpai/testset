<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keykeep.app">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:name=".application.KeyKeepApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".views.activity.SplashActivity"
            android:theme="@style/NoActionbarTheme"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.keykeep.app.views.activity.login.LoginActivity"
            android:theme="@style/NoActionbarTheme" />

        <activity
            android:name="com.keykeep.app.views.activity.forgot_password.ForgotPasswordActivity"
            android:theme="@style/NoActionbarTheme" />

        <activity
            android:name="com.keykeep.app.views.activity.home.HomeActivity"
            android:theme="@style/NoActionbarTheme" />

        <activity android:name=".views.activity.AssetListActivity" />
        <activity android:name=".qrcodescanner.QrCodeActivity" />
        <activity android:name="com.keykeep.app.views.activity.assetDetail.AssetDetailActivity" />

    </application>
</manifest>